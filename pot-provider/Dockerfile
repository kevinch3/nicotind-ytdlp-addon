# NicotinD's own build of the bgutil YouTube PO-token provider, published as
# ghcr.io/kevinch3/nicotind-pot-provider. It runs as a sidecar sharing the
# network namespace of this addon (and of nicotind-spotdl-addon, which consumes
# the same published image).
#
# why here: the pin that can break a download is the `bgutil-ytdlp-pot-provider`
# pip plugin baked into the addon image — the server must match it (NicotinD
# #238, #551). The server used to be built by NicotinD core's release, a repo
# that cannot read the addon's pin; it moved here so both halves live in one
# repo and `src/pot-provider-pin.test.ts` can hold them together (NicotinD #1315).
#
# Upstream is GPL-3.0, compatible with this project's AGPL-3.0-only.
#
# Source is fetched at the pinned tag rather than vendored: vendoring a whole
# Node service would put its dependency updates on us.
# renovate: datasource=github-releases depName=Brainicism/bgutil-ytdlp-pot-provider
ARG BGUTIL_VERSION=1.3.1

FROM node:25-bookworm-slim AS source
ARG BGUTIL_VERSION
RUN apt-get update \
 && apt-get install -y --no-install-recommends ca-certificates curl \
 && rm -rf /var/lib/apt/lists/*
WORKDIR /src
RUN curl -fsSL "https://github.com/Brainicism/bgutil-ytdlp-pot-provider/archive/refs/tags/${BGUTIL_VERSION}.tar.gz" \
  | tar -xz --strip-components=1

# The three stages below mirror upstream's own server/Dockerfile (node target).
# Kept deliberately close to it so a version bump is a tag change, not a rewrite.
FROM node:25-bookworm-slim AS install
# WORKDIR creates /app owned by root, so chown it before dropping privileges —
# otherwise `npm ci` can't mkdir node_modules. (Upstream gets away without this
# because its BuildKit cache mount side-steps the ordering.)
RUN mkdir -p /app && chown node:node /app
USER node
WORKDIR /app
# Upstream uses BuildKit cache mounts for the npm cache; this build must also
# work on a plain (non-buildx) daemon, so point the cache somewhere the `node`
# user can always write instead of depending on that mount existing.
ENV NPM_CONFIG_CACHE=/tmp/.npm
COPY --from=source --chown=node:node /src/server/package.json /src/server/package-lock.json ./
RUN npm ci --omit=dev --no-audit --no-fund

FROM install AS build_node
RUN npm ci --no-audit --no-fund
COPY --from=source --chown=node:node /src/server/types/ types/
COPY --from=source --chown=node:node /src/server/tsconfig.json ./
COPY --from=source --chown=node:node /src/server/src/ src/
RUN npx tsc

FROM install AS server
# Republish the pin ON the artifact (NicotinD #551). The plugin baked into the
# spotdl addon image must match the server version here too, and that repo
# cannot read this Dockerfile — but any build can inspect a label. A source-file
# check would also pass while the *published* image is stale; this cannot.
# ARG is re-declared because a top-level one is out of scope inside a stage —
# without this the label interpolates to an empty string.
ARG BGUTIL_VERSION
LABEL org.nicotind.bgutil.version="${BGUTIL_VERSION}"
COPY --from=build_node /app/build /app/build
EXPOSE 4416
ENTRYPOINT ["/usr/local/bin/node", "build/main.js"]
